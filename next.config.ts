import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Internal GTM dashboard. Access is controlled by Vercel Deployment
  // Protection (Project Settings -> Deployment Protection), not app code.
};

export default nextConfig;
